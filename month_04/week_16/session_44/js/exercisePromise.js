const exercisePromise = new Promise((resolve, rejected)=>{
    const isTrue = true;
    if (isTrue) {
        resolve();
    } else{
        rejected();
    }
});


console.log(exercisePromise);

exercisePromise.then(()=>{










})
